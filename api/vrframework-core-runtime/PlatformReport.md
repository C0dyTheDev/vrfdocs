---
id: PlatformReport
title: 'PlatformReport class'
sidebar_label: 'PlatformReport'
description: 'Everything the session report is made of.'
# Generated by scripts/gen-api.mjs - edit the C# doc comments instead.
---

# PlatformReport

**Class** · namespace `VRFramework.Core.Runtime` · assembly `VRFramework.Core.Runtime` · [view source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Core/PlatformReport.cs#L19)

Everything the session report is made of. The report is not a fixed shape: it is whatever
the [`IReportContributor`](/api/vrframework-core-runtime/IReportContributor)s in the loaded scenes hand over, plus whatever was
put in by hand through [`SetValue(string, object)`](/api/vrframework-core-runtime/PlatformReport#setvalue-string-object). PlatformManager collects, serializes and
posts it - this class only decides what is in it.

A contributor that lives in a scene which unloads before the session ends (the parts of
a minigame chain, for instance) would take its data with it. [`Freeze(IReportContributor)`](/api/vrframework-core-runtime/PlatformReport#freeze-ireportcontributor)
keeps the value behind: [`ReportContributor`](/api/vrframework-core-runtime/ReportContributor) does that on its way out, and
PlatformManager freezes everything when a minigame part hands over to the next scene.

```csharp
public static class PlatformReport
```

**Inheritance:** [`object`](https://learn.microsoft.com/dotnet/api/system.object) ← `PlatformReport`

## Fields

### AutoDiscover {#autodiscover}

Whether components implementing [`IReportContributor`](/api/vrframework-core-runtime/IReportContributor) are picked up from the
loaded scenes without registering. Turn off to report only what registered explicitly.

```csharp
public static bool AutoDiscover
```

**Returns** [`bool`](https://learn.microsoft.com/dotnet/api/system.boolean)

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Core/PlatformReport.cs#L31)

## Methods

### Clear() {#clear}

Forgets every contributor and every stored value.

```csharp
public static void Clear()
```

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Core/PlatformReport.cs#L108)

### Collect() {#collect}

Everything that goes into the report right now, in the order it will be written:
stored values first, then what the live contributors say.

```csharp
public static List<ReportEntry> Collect()
```

**Returns** `List<ReportEntry>` - One entry per report key, in write order.

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Core/PlatformReport.cs#L152)

### Freeze(IReportContributor) {#freeze-ireportcontributor}

Stores what a contributor holds right now, so the key stays in the report even once the
contributor is gone. A later value for the same key - from a contributor still alive
when the report is built - wins over the frozen one.

```csharp
public static void Freeze(IReportContributor contributor)
```

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `contributor` | [`IReportContributor`](/api/vrframework-core-runtime/IReportContributor) | Contributor whose current value is kept behind. |

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Core/PlatformReport.cs#L125)

### FreezeAll() {#freezeall}

Freezes every contributor currently alive. Worth doing whenever the scene is about to be
replaced and the session carries on.

```csharp
public static void FreezeAll()
```

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Core/PlatformReport.cs#L136)

### GetValue(string) {#getvalue-string}

Reads back a value set with [`SetValue(string, object)`](/api/vrframework-core-runtime/PlatformReport#setvalue-string-object). Null when there is none.

```csharp
public static object GetValue(string key)
```

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `key` | [`string`](https://learn.microsoft.com/dotnet/api/system.string) | Key to read. |

**Returns** [`object`](https://learn.microsoft.com/dotnet/api/system.object) - The stored value, or null when the key holds none.

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Core/PlatformReport.cs#L92)

### Register(IReportContributor) {#register-ireportcontributor}

Announces a contributor that auto-discovery cannot see - anything that is not a
component. Components may register too; registering twice is harmless.

```csharp
public static void Register(IReportContributor contributor)
```

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `contributor` | [`IReportContributor`](/api/vrframework-core-runtime/IReportContributor) | Contributor to announce. |

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Core/PlatformReport.cs#L48)

### Remove(string) {#remove-string}

Takes a stored value back out of the report. Contributors are unaffected.

```csharp
public static void Remove(string key)
```

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `key` | [`string`](https://learn.microsoft.com/dotnet/api/system.string) | Key to remove. |

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Core/PlatformReport.cs#L99)

### SetValue(string, object) {#setvalue-string-object}

Puts a value into the report by hand and keeps it there for the rest of the session,
scene loads included. The counterpart to a contributor, for one-off facts that no
object is around to report later.

```csharp
public static void SetValue(string key, object value)
```

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `key` | [`string`](https://learn.microsoft.com/dotnet/api/system.string) | Key it lands under in the report JSON. |
| `value` | [`object`](https://learn.microsoft.com/dotnet/api/system.object) | Anything JSON can hold, or a [`RawJson`](/api/vrframework-core-runtime/RawJson) when it is already written. |

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Core/PlatformReport.cs#L74)

### Unregister(IReportContributor) {#unregister-ireportcontributor}

Withdraws a contributor. Its frozen values, if any, stay in the report.

```csharp
public static void Unregister(IReportContributor contributor)
```

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `contributor` | [`IReportContributor`](/api/vrframework-core-runtime/IReportContributor) | Contributor to withdraw. |

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Core/PlatformReport.cs#L60)

