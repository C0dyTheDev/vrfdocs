---
id: IReportContributor
title: 'IReportContributor interface'
sidebar_label: 'IReportContributor'
description: 'One slice of the session report.'
# Generated by scripts/gen-api.mjs - edit the C# doc comments instead.
---

# IReportContributor

**Interface** · namespace `VRFramework.Core.Runtime` · assembly `VRFramework.Core.Runtime` · [view source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Core/IReportContributor.cs#L11)

One slice of the session report. Anything can supply one - a module, a plain component, a
class of the app's own - and the report the platform receives is the sum of them, so what
gets reported is decided by the app rather than fixed by the framework.

Components are found automatically wherever they sit in the loaded scenes. Everything
else announces itself with [`Register(IReportContributor)`](/api/vrframework-core-runtime/PlatformReport#register-ireportcontributor).

```csharp
public interface IReportContributor
```

**Derived:** [`ReportContributor`](/api/vrframework-core-runtime/ReportContributor), [`MistakesManager`](/api/vrframework-mistakes-runtime/MistakesManager)

## Properties

### ReportKey {#reportkey}

The key this contribution lands under in the report JSON. Two contributors claiming the
same key is a conflict: the framework says so and the last one collected wins.

```csharp
string ReportKey { get; }
```

**Returns** [`string`](https://learn.microsoft.com/dotnet/api/system.string)

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Core/IReportContributor.cs#L17)

## Methods

### GetReportValue() {#getreportvalue}

The value for [`ReportKey`](/api/vrframework-core-runtime/IReportContributor#reportkey). Anything JSON can hold: a string, a number, a
bool, a [Serializable] class, a List or Dictionary of those, or a [`RawJson`](/api/vrframework-core-runtime/RawJson)
when the JSON is already written. Returning null leaves the key out of the report.

```csharp
object GetReportValue()
```

**Returns** [`object`](https://learn.microsoft.com/dotnet/api/system.object)

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Core/IReportContributor.cs#L24)

