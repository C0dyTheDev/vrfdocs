---
id: GraphUpdater
title: 'GraphUpdater class'
sidebar_label: 'GraphUpdater'
description: 'Coalesces graph redraw requests, so everything that dirties the scenario within one tick costs a single redraw.'
# Generated by scripts/gen-api.mjs - edit the C# doc comments instead.
---

# GraphUpdater

**Class** · namespace `VRFramework.Progress.Runtime` · assembly `VRFramework.Progress.Runtime` · [view source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Progress/GraphUpdater.cs#L13)

Coalesces graph redraw requests, so everything that dirties the scenario within one tick
costs a single redraw. The work is editor-only, but the component sits on the Scenario object
in the scene, so the class itself still has to compile into a build.

```csharp
[ExecuteAlways]
[DisallowMultipleComponent]
public class GraphUpdater : MonoBehaviour
```

**Inheritance:** [`object`](https://learn.microsoft.com/dotnet/api/system.object) ← [`Object`](https://docs.unity3d.com/ScriptReference/Object.html) ← [`Component`](https://docs.unity3d.com/ScriptReference/Component.html) ← [`Behaviour`](https://docs.unity3d.com/ScriptReference/Behaviour.html) ← [`MonoBehaviour`](https://docs.unity3d.com/ScriptReference/MonoBehaviour.html) ← `GraphUpdater`

## Methods

### SetDirty() {#setdirty}

```csharp
public static void SetDirty()
```

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Progress/GraphUpdater.cs#L17)

