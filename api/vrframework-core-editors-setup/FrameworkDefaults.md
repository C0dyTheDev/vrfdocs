---
id: FrameworkDefaults
title: 'FrameworkDefaults class'
sidebar_label: 'FrameworkDefaults'
description: 'What a set-up project looks like, in one place.'
# Generated by scripts/gen-api.mjs - edit the C# doc comments instead.
---

# FrameworkDefaults

**Class** · namespace `VRFramework.Core.Editors.Setup` · assembly `VRFramework.Core.Editors` · [view source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Editor/Setup/FrameworkDefaults.cs#L8)

What a set-up project looks like, in one place. [`ProjectSetup`](/api/vrframework-core-editors-setup/ProjectSetup) writes these values
and the validator's project rules check for them, so they have to agree: when the two each kept
their own copy, changing one of them left the setup writing a value the validator then reported
as wrong.

```csharp
public static class FrameworkDefaults
```

**Inheritance:** [`object`](https://learn.microsoft.com/dotnet/api/system.object) ← `FrameworkDefaults`

## Fields

### BuildSettingsPath {#buildsettingspath}

Build flags PlatformManager reads on every start.

```csharp
public const string BuildSettingsPath = "Assets/StreamingAssets/BuildSettings.json"
```

**Returns** [`string`](https://learn.microsoft.com/dotnet/api/system.string)

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Editor/Setup/FrameworkDefaults.cs#L20)

### BuildSettingsTemplate {#buildsettingstemplate}

The BuildSettings.json a project starts with: developing, not shipping.

```csharp
public const string BuildSettingsTemplate = "{\"isProductionBuild\":false}"
```

**Returns** [`string`](https://learn.microsoft.com/dotnet/api/system.string)

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Editor/Setup/FrameworkDefaults.cs#L26)

### ExecutionOrders {#executionorders}

VRCore has to Awake before the modules it drives, and Progress before scene scripts.

```csharp
public static readonly (string script, int order)[] ExecutionOrders
```

**Returns** `(string script, int order)[]`

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Editor/Setup/FrameworkDefaults.cs#L62)

### FixedTimestep {#fixedtimestep}

Physics at 120 Hz, so a grabbed object stays put at headset frame rates.

```csharp
public const float FixedTimestep = 0.008333
```

**Returns** [`float`](https://learn.microsoft.com/dotnet/api/system.single)

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Editor/Setup/FrameworkDefaults.cs#L11)

### IgnoredCollisions {#ignoredcollisions}

Layer pairs that must not collide, with what goes wrong when they do - the validator says
that part out loud, the setup only needs the pair.

```csharp
public static readonly (string a, string b, string consequence)[] IgnoredCollisions
```

**Returns** `(string a, string b, string consequence)[]`

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Editor/Setup/FrameworkDefaults.cs#L38)

### MyDataPath {#mydatapath}

What the platform reads to learn which scenes and parameters the app offers.

```csharp
public const string MyDataPath = "Assets/StreamingAssets/MyData.json"
```

**Returns** [`string`](https://learn.microsoft.com/dotnet/api/system.string)

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Editor/Setup/FrameworkDefaults.cs#L23)

### RequiredCollisions {#requiredcollisions}

Layer pairs that must collide, with what goes wrong when they do not.

Grabbable against itself used to be on the list above, because the old hold moved a held
object by writing its transform and a transform does not yield to anything it meets. The
hold writes velocity now and gives ground, so the reason is gone - and the cost of the
workaround was never only about held objects: it is every grabbable in the scene passing
through every other one, on a shelf, in a crate, with nobody holding either of them.

```csharp
public static readonly (string a, string b, string consequence)[] RequiredCollisions
```

**Returns** `(string a, string b, string consequence)[]`

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Editor/Setup/FrameworkDefaults.cs#L54)

### RequiredLayers {#requiredlayers}

Layers the framework puts objects on, and the physics matrix is built around.

```csharp
public static readonly string[] RequiredLayers
```

**Returns** [`string[]`](https://learn.microsoft.com/dotnet/api/system.string[])

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Editor/Setup/FrameworkDefaults.cs#L32)

### RequiredTags {#requiredtags}

Tags the framework assigns to objects. GrabbableObject.Reset writes this one.

```csharp
public static readonly string[] RequiredTags
```

**Returns** [`string[]`](https://learn.microsoft.com/dotnet/api/system.string[])

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Editor/Setup/FrameworkDefaults.cs#L29)

### SolverIterations {#solveriterations}

Grabbing and joints jitter below this; the framework is tuned at 10.

```csharp
public const int SolverIterations = 10
```

**Returns** [`int`](https://learn.microsoft.com/dotnet/api/system.int32)

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Editor/Setup/FrameworkDefaults.cs#L14)

### StreamingAssetsFolder {#streamingassetsfolder}

Where the two files the platform reads on start live.

```csharp
public const string StreamingAssetsFolder = "Assets/StreamingAssets"
```

**Returns** [`string`](https://learn.microsoft.com/dotnet/api/system.string)

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Editor/Setup/FrameworkDefaults.cs#L17)

