---
id: PackageSetup
title: 'PackageSetup class'
sidebar_label: 'PackageSetup'
description: 'Takes a project from empty to buildable on an Android headset in one menu item.'
# Generated by scripts/gen-api.mjs - edit the C# doc comments instead.
---

# PackageSetup

**Class** · namespace `VRFramework.Core.Editors.Setup` · assembly `VRFramework.Core.Editors` · [view source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Editor/Setup/PackageSetup.cs#L21)

Takes a project from empty to buildable on an Android headset in one menu item.
This used to be three menu items that had to be clicked in order, because switching platform,
installing a package and importing a sample each recompile the project and a recompile threw
away everything the setup was in the middle of. The work itself was never the problem; losing
track of it was. So the sequence below is a plain ordered list and
[`SetupRunner`](/api/vrframework-core-editors-setup/SetupRunner) carries the position through the reloads.
Adding a step means adding one line here. A project adds steps of its own without touching
this file, by subclassing [`SetupExtension`](/api/vrframework-core-editors-setup/SetupExtension); those run at the end, after the
framework has finished with the project.
Nothing here draws anything. Starting, watching and cancelling a run all happen in
[`PackageSetupWindow`](/api/vrframework-core-editors-setup/PackageSetupWindow), which is also the menu item.

```csharp
public static class PackageSetup
```

**Inheritance:** [`object`](https://learn.microsoft.com/dotnet/api/system.object) ← `PackageSetup`

## Methods

### ApplyEditorLayout() {#applyeditorlayout}

Kept out of the sequence on purpose. It rearranges the editor's own windows, which is a
matter of taste rather than of the project building, and doing it mid-setup would close
the console the setup is reporting into.

```csharp
[MenuItem("VRFramework/Appearance/Apply VRF Editor Layout", priority = 100)]
public static void ApplyEditorLayout()
```

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Editor/Setup/PackageSetup.cs#L58)

### Begin() {#begin}

Starts the whole sequence. The window asks the question first; by the time this is called
the answer was yes.

```csharp
public static void Begin()
```

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Editor/Setup/PackageSetup.cs#L39)

### Preview() {#preview}

The steps a run would take, for listing them before anyone has pressed anything. Building
the sequence has no side effects - the work is inside the steps, not in making them.

```csharp
public static IReadOnlyList<SetupStep> Preview()
```

**Returns** `IReadOnlyList<SetupStep>`

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Editor/Setup/PackageSetup.cs#L48)

