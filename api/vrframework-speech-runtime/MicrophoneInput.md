---
id: MicrophoneInput
title: 'MicrophoneInput class'
sidebar_label: 'MicrophoneInput'
description: 'The headset microphone, read as a rolling buffer.'
# Generated by scripts/gen-api.mjs - edit the C# doc comments instead.
---

# MicrophoneInput

**Class** · namespace `VRFramework.Speech.Runtime` · assembly `VRFramework.Speech.Runtime` · [view source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Speech/MicrophoneInput.cs#L11)

The headset microphone, read as a rolling buffer. Unity records into a short looping clip and
reports a write position; this hands on whatever has been written since it was last asked.

```csharp
public class MicrophoneInput : SpeechAudioInput
```

**Inheritance:** [`object`](https://learn.microsoft.com/dotnet/api/system.object) ← [`Object`](https://docs.unity3d.com/ScriptReference/Object.html) ← [`Component`](https://docs.unity3d.com/ScriptReference/Component.html) ← [`Behaviour`](https://docs.unity3d.com/ScriptReference/Behaviour.html) ← [`MonoBehaviour`](https://docs.unity3d.com/ScriptReference/MonoBehaviour.html) ← [`SpeechAudioInput`](/api/vrframework-speech-runtime/SpeechAudioInput) ← `MicrophoneInput`

## Fields

### deviceIndex {#deviceindex}

Index into [`devices`](https://docs.unity3d.com/ScriptReference/Microphone.devices.html). 0 is the headset's own microphone.

```csharp
[Tooltip("Index into the system's microphone list. 0 is the default device.")]
public int deviceIndex
```

**Returns** [`int`](https://learn.microsoft.com/dotnet/api/system.int32)

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Speech/MicrophoneInput.cs#L21)

### sampleRate {#samplerate}

Rate the microphone is opened at. The shipped model is trained on 16 kHz; recording at
another rate feeds the recogniser audio it cannot read.

```csharp
[Tooltip("Recording rate in Hz. The speech model expects 16000.")]
public int sampleRate
```

**Returns** [`int`](https://learn.microsoft.com/dotnet/api/system.int32)

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Speech/MicrophoneInput.cs#L28)

## Properties

### IsCapturing {#iscapturing}

Whether audio is arriving right now. The manager holds the recogniser idle while this is
false, so silence from a stopped microphone is never mistaken for silence from a quiet room.

```csharp
public override bool IsCapturing { get; }
```

**Returns** [`bool`](https://learn.microsoft.com/dotnet/api/system.boolean)

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Speech/MicrophoneInput.cs#L38)

### SampleRate {#samplerate}

Sample rate of what [`ReadSamples()`](/api/vrframework-speech-runtime/SpeechAudioInput#readsamples) returns, in Hz.

```csharp
public override int SampleRate { get; }
```

**Returns** [`int`](https://learn.microsoft.com/dotnet/api/system.int32)

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Speech/MicrophoneInput.cs#L35)

## Methods

### ReadSamples() {#readsamples}

The samples captured since the last call, as mono values in -1 to 1.

```csharp
public override float[] ReadSamples()
```

**Returns** [`float[]`](https://learn.microsoft.com/dotnet/api/system.single[]) - The new samples, or null when there are none yet.

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Speech/MicrophoneInput.cs#L69)

