---
id: SpeechHotword
title: 'SpeechHotword class'
sidebar_label: 'SpeechHotword'
description: 'A word the recogniser is told to favour, and how strongly.'
# Generated by scripts/gen-api.mjs - edit the C# doc comments instead.
---

# SpeechHotword

**Class** · namespace `VRFramework.Speech.Runtime` · assembly `VRFramework.Speech.Runtime` · [view source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Speech/SpeechRecognitionManager.cs#L14)

A word the recogniser is told to favour, and how strongly.

```csharp
[Serializable]
public class SpeechHotword
```

**Inheritance:** [`object`](https://learn.microsoft.com/dotnet/api/system.object) ← `SpeechHotword`

## Fields

### boost {#boost}

How much to favour it. Around 2 to 3 pulls a rare technical term out of a sentence the
model would otherwise turn into common words; much higher and it starts hearing it
everywhere.

```csharp
[Range(0, 10)]
public float boost
```

**Returns** [`float`](https://learn.microsoft.com/dotnet/api/system.single)

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Speech/SpeechRecognitionManager.cs#L25)

### word {#word}

The word, spelled as the model's tokens spell it.

```csharp
public string word
```

**Returns** [`string`](https://learn.microsoft.com/dotnet/api/system.string)

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Speech/SpeechRecognitionManager.cs#L18)

