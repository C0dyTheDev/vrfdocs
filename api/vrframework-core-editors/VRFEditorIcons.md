---
id: VRFEditorIcons
title: 'VRFEditorIcons class'
sidebar_label: 'VRFEditorIcons'
description: 'The framework''s one cache of Unity''s built-in editor icons, shared by every VRF window.'
# Generated by scripts/gen-api.mjs - edit the C# doc comments instead.
---

# VRFEditorIcons

**Class** · namespace `VRFramework.Core.Editors` · assembly `VRFramework.Core.Editors` · [view source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Editor/VRFEditorIcons.cs#L13)

The framework's one cache of Unity's built-in editor icons, shared by every VRF window.
IconContent is not free and it throws on names that do not exist in the running editor
version, so nothing should call it directly: rows and cards ask for the same handful of
icons over and over while scrolling.

```csharp
public static class VRFEditorIcons
```

**Inheritance:** [`object`](https://learn.microsoft.com/dotnet/api/system.object) ← `VRFEditorIcons`

## Methods

### Builtin(string) {#builtin-string}

Built-in editor icon by name, or null when this editor version does not have it.
Unlike EditorGUIUtility.IconContent, an unknown name is not an error.

```csharp
public static Texture Builtin(string iconName)
```

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `iconName` | [`string`](https://learn.microsoft.com/dotnet/api/system.string) |  |

**Returns** [`Texture`](https://docs.unity3d.com/ScriptReference/Texture.html)

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Editor/VRFEditorIcons.cs#L21)

### ClearCache() {#clearcache}

Drops the cache. Icons change with the editor skin, so a theme switch wants this.

```csharp
public static void ClearCache()
```

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Editor/VRFEditorIcons.cs#L43)

