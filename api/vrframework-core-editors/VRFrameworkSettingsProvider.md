---
id: VRFrameworkSettingsProvider
title: 'VRFrameworkSettingsProvider class'
sidebar_label: 'VRFrameworkSettingsProvider'
description: 'The VR Framework pages in Project Settings.'
# Generated by scripts/gen-api.mjs - edit the C# doc comments instead.
---

# VRFrameworkSettingsProvider

**Class** · namespace `VRFramework.Core.Editors` · assembly `VRFramework.Core.Editors` · [view source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Editor/VRFrameworkSettingsProvider.cs#L22)

The VR Framework pages in Project Settings. One provider class serves every page: the root
node and each of its children are the same code driven by a different entry in
`Pages`.

Adding a page means adding one entry to that table. Adding a setting means adding the
field to [`VRFrameworkSettings`](/api/vrframework-core-editors/VRFrameworkSettings) and naming it on a page - and forgetting the
second step is not fatal, because any field no page claims is still drawn on the root
page rather than silently disappearing.

```csharp
public class VRFrameworkSettingsProvider : SettingsProvider
```

**Inheritance:** [`object`](https://learn.microsoft.com/dotnet/api/system.object) ← [`SettingsProvider`](https://docs.unity3d.com/ScriptReference/SettingsProvider.html) ← `VRFrameworkSettingsProvider`

## Methods

### CreateSettingsProviders() {#createsettingsproviders}

```csharp
[SettingsProviderGroup]
public static SettingsProvider[] CreateSettingsProviders()
```

**Returns** [`SettingsProvider[]`](https://docs.unity3d.com/ScriptReference/SettingsProvider[].html)

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Editor/VRFrameworkSettingsProvider.cs#L324)

### OnActivate(string, VisualElement) {#onactivate-string-visualelement}

Use this function to implement a handler for when the user clicks on the Settings in the Settings window. You can fetch a settings Asset or set up UIElements UI from this function.

```csharp
public override void OnActivate(string searchContext, VisualElement rootElement)
```

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `searchContext` | [`string`](https://learn.microsoft.com/dotnet/api/system.string) | Search context in the search box on the Settings window. |
| `rootElement` | [`VisualElement`](https://docs.unity3d.com/ScriptReference/UIElements.VisualElement.html) | Root of the UIElements tree. If you add to this root, the SettingsProvider uses UIElements instead of calling SettingsProvider.OnGUI to build the UI. If you do not add to this VisualElement, then you must use the IMGUI to build the UI. |

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Editor/VRFrameworkSettingsProvider.cs#L74)

### OnDeactivate() {#ondeactivate}

Saving is deferred to here. The page used to call AssetDatabase.SaveAssets on every
change, which writes the asset to disk on each keystroke in a text field.

```csharp
public override void OnDeactivate()
```

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Editor/VRFrameworkSettingsProvider.cs#L103)

