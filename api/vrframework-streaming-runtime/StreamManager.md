---
id: StreamManager
title: 'StreamManager class'
sidebar_label: 'StreamManager'
description: 'The streaming module.'
# Generated by scripts/gen-api.mjs - edit the C# doc comments instead.
---

# StreamManager

**Class** · namespace `VRFramework.Streaming.Runtime` · assembly `VRFramework.Streaming.Runtime` · [view source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Streaming/StreamManager.cs#L15)

The streaming module. Streams what a camera sees to a viewer on the network: renders to a
texture, reads it back off the GPU without stalling the frame, encodes it as JPEG on a worker
thread and sends it over UDP. Frames are only produced while somebody is actually watching.
Registers itself as [`IStreamingService`](/api/vrframework-core-runtime/IStreamingService).

```csharp
public class StreamManager : VRModule, IStreamingService
```

**Inheritance:** [`object`](https://learn.microsoft.com/dotnet/api/system.object) ← [`Object`](https://docs.unity3d.com/ScriptReference/Object.html) ← [`Component`](https://docs.unity3d.com/ScriptReference/Component.html) ← [`Behaviour`](https://docs.unity3d.com/ScriptReference/Behaviour.html) ← [`MonoBehaviour`](https://docs.unity3d.com/ScriptReference/MonoBehaviour.html) ← [`VRModule`](/api/vrframework-core-runtime/VRModule) ← `StreamManager`

**Implements:** [`IStreamingService`](/api/vrframework-core-runtime/IStreamingService)

## Fields

### FPS {#fps}

Frames sent per second.

```csharp
public int FPS
```

**Returns** [`int`](https://learn.microsoft.com/dotnet/api/system.int32)

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Streaming/StreamManager.cs#L18)

### height {#height}

Height of the streamed image, in pixels.

```csharp
public int height
```

**Returns** [`int`](https://learn.microsoft.com/dotnet/api/system.int32)

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Streaming/StreamManager.cs#L22)

### quality {#quality}

JPEG quality, as 0 to 100.

```csharp
public int quality
```

**Returns** [`int`](https://learn.microsoft.com/dotnet/api/system.int32)

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Streaming/StreamManager.cs#L24)

### streamCamera {#streamcamera}

The camera whose view is streamed.

```csharp
public Camera streamCamera
```

**Returns** [`Camera`](https://docs.unity3d.com/ScriptReference/Camera.html)

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Streaming/StreamManager.cs#L27)

### width {#width}

Width of the streamed image, in pixels.

```csharp
public int width
```

**Returns** [`int`](https://learn.microsoft.com/dotnet/api/system.int32)

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Streaming/StreamManager.cs#L20)

## Properties

### DefaultInitOrder {#defaultinitorder}

Initialises after every other module: what it streams is the finished scene, so nothing is
gained by starting the sockets before the rest of the scene stands.

```csharp
public override int DefaultInitOrder { get; }
```

**Returns** [`int`](https://learn.microsoft.com/dotnet/api/system.int32)

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Streaming/StreamManager.cs#L50)

### FramesSent {#framessent}

How many frames have been sent since the module started.

```csharp
public int FramesSent { get; }
```

**Returns** [`int`](https://learn.microsoft.com/dotnet/api/system.int32)

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Streaming/StreamManager.cs#L59)

### IsWatched {#iswatched}

True while the viewer's heartbeat is still arriving, i.e. somebody is watching. False before
the module has started as well.

```csharp
public bool IsWatched { get; }
```

**Returns** [`bool`](https://learn.microsoft.com/dotnet/api/system.boolean)

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Streaming/StreamManager.cs#L56)

### StreamCamera {#streamcamera}

The camera whose view is being streamed.

```csharp
public Camera StreamCamera { get; }
```

**Returns** [`Camera`](https://docs.unity3d.com/ScriptReference/Camera.html)

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Streaming/StreamManager.cs#L62)

## Methods

### OnCleanup() {#oncleanup}

Unregisters the module, unless another one has taken over the service in the meantime.

```csharp
public override void OnCleanup()
```

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Streaming/StreamManager.cs#L130)

### OnInit() {#oninit}

Sets the stream up and registers the service. A module with no camera to stream switches
itself off rather than failing every capture.

```csharp
public override void OnInit()
```

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Streaming/StreamManager.cs#L91)

### StreamFrom(Camera) {#streamfrom-camera}

Streams a different camera's view from the next capture on. The render texture is unchanged,
so the resolution stays what the module was set up with.

```csharp
public void StreamFrom(Camera camera)
```

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `camera` | [`Camera`](https://docs.unity3d.com/ScriptReference/Camera.html) | The camera to stream. A null camera is refused and changes nothing. |

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Streaming/StreamManager.cs#L69)

