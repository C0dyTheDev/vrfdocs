---
id: CountToggle
title: 'CountToggle class'
sidebar_label: 'CountToggle'
description: 'Icon plus running count, the way the Unity console shows its severity switches.'
# Generated by scripts/gen-api.mjs - edit the C# doc comments instead.
---

# CountToggle

**Class** · namespace `VRFramework.Core.Editors.UI` · assembly `VRFramework.Core.Editors` · [view source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Editor/UI/CountToggle.cs#L13)

Icon plus running count, the way the Unity console shows its severity switches. Built by hand
rather than from ToolbarToggle so the icon sits before the number.
The USS class names are passed in rather than baked in, so the VRF Logger and the Validator
each keep their own look while sharing one switch: the two used to carry a copy of this each,
and a fix to one of them never reached the other.

```csharp
public sealed class CountToggle : VisualElement, IEventHandler, IResolvedStyle, ITransform, ITransitionAnimations, IExperimentalFeatures, IVisualElementScheduler, ICustomStyle
```

**Inheritance:** [`object`](https://learn.microsoft.com/dotnet/api/system.object) ← [`CallbackEventHandler`](https://docs.unity3d.com/ScriptReference/UIElements.CallbackEventHandler.html) ← [`Focusable`](https://docs.unity3d.com/ScriptReference/UIElements.Focusable.html) ← [`VisualElement`](https://docs.unity3d.com/ScriptReference/UIElements.VisualElement.html) ← `CountToggle`

**Implements:** [`IEventHandler`](https://docs.unity3d.com/ScriptReference/UIElements.IEventHandler.html), [`IResolvedStyle`](https://docs.unity3d.com/ScriptReference/UIElements.IResolvedStyle.html), [`ITransform`](https://docs.unity3d.com/ScriptReference/UIElements.ITransform.html), [`ITransitionAnimations`](https://docs.unity3d.com/ScriptReference/UIElements.Experimental.ITransitionAnimations.html), [`IExperimentalFeatures`](https://docs.unity3d.com/ScriptReference/UIElements.IExperimentalFeatures.html), [`IVisualElementScheduler`](https://docs.unity3d.com/ScriptReference/UIElements.IVisualElementScheduler.html), [`ICustomStyle`](https://docs.unity3d.com/ScriptReference/UIElements.ICustomStyle.html)

## Constructors

### CountToggle(string, Texture, string, bool, Action\<bool>) {#ctor-string-texture-string-boolean-boolean}

Builds a switch.

```csharp
public CountToggle(string ussClass, Texture icon, string tooltipText, bool value, Action<bool> onChange)
```

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `ussClass` | [`string`](https://learn.microsoft.com/dotnet/api/system.string) | Base USS class; the icon, count and on-state classes are derived from it. |
| `icon` | [`Texture`](https://docs.unity3d.com/ScriptReference/Texture.html) | Image shown before the count. |
| `tooltipText` | [`string`](https://learn.microsoft.com/dotnet/api/system.string) | What the switch shows or hides. |
| `value` | [`bool`](https://learn.microsoft.com/dotnet/api/system.boolean) | Whether it starts switched on. |
| `onChange` | `Action<bool>` | Called when the user flips it. |

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Editor/UI/CountToggle.cs#L26)

## Methods

### SetCount(int) {#setcount-int32}

Updates the count shown next to the icon.

```csharp
public void SetCount(int count)
```

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `count` | [`int`](https://learn.microsoft.com/dotnet/api/system.int32) | How many entries of this kind there are. |

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Editor/UI/CountToggle.cs#L54)

