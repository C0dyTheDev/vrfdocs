---
id: GripConstraintOneAxis
title: 'GripConstraintOneAxis class'
sidebar_label: 'GripConstraintOneAxis'
description: 'Lets the object turn about one axis, or slide along one, while this grip point is held.'
# Generated by scripts/gen-api.mjs - edit the C# doc comments instead.
---

# GripConstraintOneAxis

**Class** · namespace `VRFramework.Interaction.Runtime` · assembly `VRFramework.Interaction.Runtime` · [view source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Interaction/GripConstraintOneAxis.cs#L19)

Lets the object turn about one axis, or slide along one, while this grip point is held.

A door, a drawer, a valve, a lever, a bolt. Two of these on one grip point make a power cell:
turn it, then pull it out - and a [`GripSequence`](/api/vrframework-interaction-runtime/GripSequence) holds the pull shut until the
turn is done.

On a grip point when the freedom depends on where the thing is held, and on the grabbable
itself when it does not. A valve with a handle either side of its hub is the second kind: one
of these on the valve, and both handles turn the same one.

```csharp
public class GripConstraintOneAxis : GripConstraint
```

**Inheritance:** [`object`](https://learn.microsoft.com/dotnet/api/system.object) ← [`Object`](https://docs.unity3d.com/ScriptReference/Object.html) ← [`Component`](https://docs.unity3d.com/ScriptReference/Component.html) ← [`Behaviour`](https://docs.unity3d.com/ScriptReference/Behaviour.html) ← [`MonoBehaviour`](https://docs.unity3d.com/ScriptReference/MonoBehaviour.html) ← [`GripConstraint`](/api/vrframework-interaction-runtime/GripConstraint) ← `GripConstraintOneAxis`

## Fields

### axis {#axis}

```csharp
[Tooltip("Which of the pivot's axes it runs along or about. Its blue arrow is Z.")]
public GripConstraintOneAxis.Axis axis
```

**Returns** [`GripConstraintOneAxis.Axis`](/api/vrframework-interaction-runtime/GripConstraintOneAxis.Axis)

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Interaction/GripConstraintOneAxis.cs#L68)

### end {#end}

```csharp
[Tooltip("The other end, in metres for a slide or degrees for a turn.")]
public GripConstraintOneAxis.Limit end
```

**Returns** [`GripConstraintOneAxis.Limit`](/api/vrframework-interaction-runtime/GripConstraintOneAxis.Limit)

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Interaction/GripConstraintOneAxis.cs#L74)

### motion {#motion}

```csharp
[Tooltip("Whether the object travels in a straight line along an axis, or turns about one.")]
public GripConstraintOneAxis.Motion motion
```

**Returns** [`GripConstraintOneAxis.Motion`](/api/vrframework-interaction-runtime/GripConstraintOneAxis.Motion)

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Interaction/GripConstraintOneAxis.cs#L65)

### start {#start}

```csharp
[Tooltip("One end of the travel. Zero is usually where the object already rests.")]
public GripConstraintOneAxis.Limit start
```

**Returns** [`GripConstraintOneAxis.Limit`](/api/vrframework-interaction-runtime/GripConstraintOneAxis.Limit)

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Interaction/GripConstraintOneAxis.cs#L71)

## Properties

### Along {#along}

The axis this runs along or about, as a direction in the pivot's own frame.

```csharp
public Vector3 Along { get; }
```

**Returns** [`Vector3`](https://docs.unity3d.com/ScriptReference/Vector3.html)

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Interaction/GripConstraintOneAxis.cs#L89)

### Max {#max}

The higher of the two ends.

```csharp
public float Max { get; }
```

**Returns** [`float`](https://learn.microsoft.com/dotnet/api/system.single)

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Interaction/GripConstraintOneAxis.cs#L83)

### Min {#min}

The lower of the two ends, whichever way round they were set.

```csharp
public float Min { get; }
```

**Returns** [`float`](https://learn.microsoft.com/dotnet/api/system.single)

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Interaction/GripConstraintOneAxis.cs#L80)

### Travel {#travel}

How far along its travel the object sits, from 0 at the start end to 1 at the other.

```csharp
public override float Travel { get; }
```

**Returns** [`float`](https://learn.microsoft.com/dotnet/api/system.single)

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Interaction/GripConstraintOneAxis.cs#L86)

### Value {#value}

How far along the travel the object sits, in metres or degrees.

```csharp
public float Value { get; }
```

**Returns** [`float`](https://learn.microsoft.com/dotnet/api/system.single)

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Interaction/GripConstraintOneAxis.cs#L77)

## Methods

### Allow(Pose, Pose) {#allow-pose-pose}

Adds this axis of freedom to the pose the object is allowed.

```csharp
protected override Pose Allow(Pose allowed, Pose wanted)
```

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `allowed` | [`Pose`](https://docs.unity3d.com/ScriptReference/Pose.html) |  |
| `wanted` | [`Pose`](https://docs.unity3d.com/ScriptReference/Pose.html) |  |

**Returns** [`Pose`](https://docs.unity3d.com/ScriptReference/Pose.html)

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Interaction/GripConstraintOneAxis.cs#L114)

### At(Pose, float) {#at-pose-single}

Where a given amount of this constraint's motion puts the object.

A turn is about the pivot rather than about the object's own origin, which is the whole
reason the pivot exists: a door swings about its hinge and the handle travels through an
arc, and taking the origin instead would spin the door on the spot.

```csharp
public Pose At(Pose from, float value)
```

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `from` | [`Pose`](https://docs.unity3d.com/ScriptReference/Pose.html) | The pose built so far. |
| `value` | [`float`](https://learn.microsoft.com/dotnet/api/system.single) | How far along, in metres or degrees. |

**Returns** [`Pose`](https://docs.unity3d.com/ScriptReference/Pose.html)

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Interaction/GripConstraintOneAxis.cs#L176)

### Engaging() {#engaging}

Records where this had got to, so travel since is added to it rather than replacing it.

```csharp
protected override void Engaging()
```

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Interaction/GripConstraintOneAxis.cs#L105)

### Hold(Pose) {#hold-pose}

Keeps the object wherever this axis had already let it get to.

```csharp
protected override Pose Hold(Pose allowed)
```

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `allowed` | [`Pose`](https://docs.unity3d.com/ScriptReference/Pose.html) |  |

**Returns** [`Pose`](https://docs.unity3d.com/ScriptReference/Pose.html)

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Interaction/GripConstraintOneAxis.cs#L161)

### MoveTo(float) {#moveto-single}

Puts the object at a given point along this travel, whatever a hand is doing.

What a door opened by a script uses, what an author previewing a mechanism uses, and what
a test drives. It writes the object rather than asking for it, so it ignores the hold - a
thing being held will be pulled back by the hand on the next step.

```csharp
public void MoveTo(float value)
```

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `value` | [`float`](https://learn.microsoft.com/dotnet/api/system.single) | How far along, in metres or degrees. |

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Interaction/GripConstraintOneAxis.cs#L199)

### ReturnToRest() {#returntorest}

Puts the object back where it started, and lets go of whichever end it was at.

```csharp
public override void ReturnToRest()
```

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Interaction/GripConstraintOneAxis.cs#L219)

