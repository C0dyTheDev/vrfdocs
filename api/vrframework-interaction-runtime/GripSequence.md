---
id: GripSequence
title: 'GripSequence class'
sidebar_label: 'GripSequence'
description: 'Joins several mechanisms into one path: which may be worked, in what order, and what happens along the way.'
# Generated by scripts/gen-api.mjs - edit the C# doc comments instead.
---

# GripSequence

**Class** · namespace `VRFramework.Interaction.Runtime` · assembly `VRFramework.Interaction.Runtime` · [view source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Interaction/GripSequence.cs#L23)

Joins several mechanisms into one path: which may be worked, in what order, and what happens along
the way.

A latch that has to be flipped before a handle will turn. A key that goes into a lock and then
turns. A pump that must come fully back before it will chamber anything. A dial that clicks
through its stops. Each of those is several places along one or more
[`GripConstraintOneAxis`](/api/vrframework-interaction-runtime/GripConstraintOneAxis), and none of them is a property of any single one.

Nothing here moves anything. The constraints move, the hold follows them, and this watches and
gates - which is the whole reason it is a separate component. A mechanism knows where its rail
is; only something above them knows which rail is allowed to be used yet.

```csharp
public class GripSequence : MonoBehaviour
```

**Inheritance:** [`object`](https://learn.microsoft.com/dotnet/api/system.object) ← [`Object`](https://docs.unity3d.com/ScriptReference/Object.html) ← [`Component`](https://docs.unity3d.com/ScriptReference/Component.html) ← [`Behaviour`](https://docs.unity3d.com/ScriptReference/Behaviour.html) ← [`MonoBehaviour`](https://docs.unity3d.com/ScriptReference/MonoBehaviour.html) ← `GripSequence`

## Fields

### onFinished {#onfinished}

```csharp
[Tooltip("Raised when the last stage is reached.")]
public UnityEvent onFinished
```

**Returns** [`UnityEvent`](https://docs.unity3d.com/ScriptReference/Events.UnityEvent.html)

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Interaction/GripSequence.cs#L83)

### order {#order}

```csharp
[Tooltip("Whether the stages have to be worked in the order they are listed.\n\nIn Order gates: a stage whose turn has not come has its mechanism switched off, so a handle will not turn until the latch is flipped. Any Order gates nothing and is how detents are described - a dial's stops, a valve's positions.")]
public GripSequence.Order order
```

**Returns** [`GripSequence.Order`](/api/vrframework-interaction-runtime/GripSequence.Order)

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Interaction/GripSequence.cs#L72)

### repeats {#repeats}

```csharp
[Tooltip("Whether finishing starts it over. On for anything that is worked again and again - a pump, a bolt - and off for anything that is done once, like a lock being opened.")]
public bool repeats
```

**Returns** [`bool`](https://learn.microsoft.com/dotnet/api/system.boolean)

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Interaction/GripSequence.cs#L80)

### stages {#stages}

```csharp
[Tooltip("The stages, in the order they must be worked when this sequence is In Order.")]
public List<GripSequence.Stage> stages
```

**Returns** `List<GripSequence.Stage>`

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Interaction/GripSequence.cs#L75)

## Properties

### Current {#current}

Which stage is being waited for. Runtime only, and meaningless out of order.

```csharp
public int Current { get; }
```

**Returns** [`int`](https://learn.microsoft.com/dotnet/api/system.int32)

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Interaction/GripSequence.cs#L86)

### IsFinished {#isfinished}

Whether every stage has been worked. Runtime only.

```csharp
public bool IsFinished { get; }
```

**Returns** [`bool`](https://learn.microsoft.com/dotnet/api/system.boolean)

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Interaction/GripSequence.cs#L89)

### Progress {#progress}

How far through the sequence this is, from 0 to 1.

```csharp
public float Progress { get; }
```

**Returns** [`float`](https://learn.microsoft.com/dotnet/api/system.single)

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Interaction/GripSequence.cs#L92)

## Methods

### Restart() {#restart}

Puts the sequence back at its first stage, and locks everything past it again.

```csharp
public void Restart()
```

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Interaction/GripSequence.cs#L155)

### Step() {#step}

Looks at where every mechanism has got to, gates the ones whose turn has not come, and raises
whatever has happened since last time.

Called from FixedUpdate, and separately callable so a test can drive it a step at a time.
Unlike a constraint, this can afford to run a step behind: it reads travel and switches
components, and neither answer changes within a step the way the gap between a hand and its
grip point does.

```csharp
public void Step()
```

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Interaction/GripSequence.cs#L114)

