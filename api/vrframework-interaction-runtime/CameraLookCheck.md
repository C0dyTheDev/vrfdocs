---
id: CameraLookCheck
title: 'CameraLookCheck class'
sidebar_label: 'CameraLookCheck'
description: 'Raises an event once the player has looked at this object for long enough - used to confirm the player has actually seen something before a step continues.'
# Generated by scripts/gen-api.mjs - edit the C# doc comments instead.
---

# CameraLookCheck

**Class** · namespace `VRFramework.Interaction.Runtime` · assembly `VRFramework.Interaction.Runtime` · [view source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Interaction/CameraLookCheck.cs#L11)

Raises an event once the player has looked at this object for long enough - used to confirm the
player has actually seen something before a step continues.

```csharp
public class CameraLookCheck : MonoBehaviour
```

**Inheritance:** [`object`](https://learn.microsoft.com/dotnet/api/system.object) ← [`Object`](https://docs.unity3d.com/ScriptReference/Object.html) ← [`Component`](https://docs.unity3d.com/ScriptReference/Component.html) ← [`Behaviour`](https://docs.unity3d.com/ScriptReference/Behaviour.html) ← [`MonoBehaviour`](https://docs.unity3d.com/ScriptReference/MonoBehaviour.html) ← `CameraLookCheck`

## Fields

### OnLookingFinished {#onlookingfinished}

Raised once the player has looked at the object for the required time.

```csharp
[Space]
public UnityEvent OnLookingFinished
```

**Returns** [`UnityEvent`](https://docs.unity3d.com/ScriptReference/Events.UnityEvent.html)

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Interaction/CameraLookCheck.cs#L22)

## Methods

### CheckIfCameraIsLooking() {#checkifcameraislooking}

Recomputes whether the player is looking at the object and starts or stops the timer. Called
every frame; public so a project can drive the check itself.

```csharp
public void CheckIfCameraIsLooking()
```

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Interaction/CameraLookCheck.cs#L84)

