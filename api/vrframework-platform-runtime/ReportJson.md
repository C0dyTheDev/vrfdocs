---
id: ReportJson
title: 'ReportJson class'
sidebar_label: 'ReportJson'
description: 'Writes what the contributors handed over as the JSON body of a report.'
# Generated by scripts/gen-api.mjs - edit the C# doc comments instead.
---

# ReportJson

**Class** · namespace `VRFramework.Platform.Runtime` · assembly `VRFramework.Platform.Runtime` · [view source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Platform/ReportJson.cs#L20)

Writes what the contributors handed over as the JSON body of a report.

Contributors return values, not text, and this is why: every string that goes in is
escaped here, so a stray quote in a mistake name or an app's own label cannot break the
payload the platform has to parse. A contributor that really does own its formatting
says so with [`RawJson`](/api/vrframework-core-runtime/RawJson).

```csharp
public static class ReportJson
```

**Inheritance:** [`object`](https://learn.microsoft.com/dotnet/api/system.object) ← `ReportJson`

## Methods

### BuildObject(IEnumerable\<ReportEntry>) {#buildobject-reportentry}

Writes the entries as one JSON object.

```csharp
public static string BuildObject(IEnumerable<ReportEntry> entries)
```

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `entries` | `IEnumerable<ReportEntry>` |  |

**Returns** [`string`](https://learn.microsoft.com/dotnet/api/system.string)

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Platform/ReportJson.cs#L27)

### Serialize(object) {#serialize-object}

Writes a single value as JSON. Same rules as a contributor's return value.

```csharp
public static string Serialize(object value)
```

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `value` | [`object`](https://learn.microsoft.com/dotnet/api/system.object) |  |

**Returns** [`string`](https://learn.microsoft.com/dotnet/api/system.string)

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Platform/ReportJson.cs#L46)

