---
id: LanguageTextInstance
title: 'LanguageTextInstance class'
sidebar_label: 'LanguageTextInstance'
description: 'An asset holding translated strings: one entry per text, each with a slot per language, plus the strings that are the same everywhere.'
# Generated by scripts/gen-api.mjs - edit the C# doc comments instead.
---

# LanguageTextInstance

**Class** · namespace `VRFramework.Localization.Runtime` · assembly `VRFramework.Localization.Runtime` · [view source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/interaction/physics-hold/Runtime/Scripts/Language/Classes/Data/Text/LanguageTextInstance.cs#L17)

An asset holding translated strings: one entry per text, each with a slot per language, plus
the strings that are the same everywhere. Referenced by the Localization module and picked
from by every [Language] text field.

```csharp
[CreateAssetMenu(fileName = "TextList", menuName = "VRFramework/Language/TextList", order = 3)]
[Serializable]
public class LanguageTextInstance : ScriptableObject
```

**Inheritance:** [`object`](https://learn.microsoft.com/dotnet/api/system.object) ← [`Object`](https://docs.unity3d.com/ScriptReference/Object.html) ← [`ScriptableObject`](https://docs.unity3d.com/ScriptReference/ScriptableObject.html) ← `LanguageTextInstance`

## Fields

### languageEntries {#languageentries}

The translated strings, one entry per text.

```csharp
[SerializeField]
public List<LanguageTextList> languageEntries
```

**Returns** `List<LanguageTextList>`

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/interaction/physics-hold/Runtime/Scripts/Language/Classes/Data/Text/LanguageTextInstance.cs#L22)

### untranslatedEntries {#untranslatedentries}

The strings that are the same in every language.

```csharp
[SerializeField]
public List<UntranslatedTextElement> untranslatedEntries
```

**Returns** `List<UntranslatedTextElement>`

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/interaction/physics-hold/Runtime/Scripts/Language/Classes/Data/Text/LanguageTextInstance.cs#L24)

## Methods

### CheckForLanguageChanges() {#checkforlanguagechanges}

Rebuilds every entry's language slots against the project's current language list, keeping the
translations already written. Run after a language is added, removed or reordered.

```csharp
public void CheckForLanguageChanges()
```

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/interaction/physics-hold/Runtime/Scripts/Language/Classes/Data/Text/LanguageTextInstance.cs#L76)

