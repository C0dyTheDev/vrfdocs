---
id: LanguageSpriteInstance
title: 'LanguageSpriteInstance class'
sidebar_label: 'LanguageSpriteInstance'
description: 'An asset holding translated sprites: one entry per image, each with a slot per language, plus the images that are the same everywhere.'
# Generated by scripts/gen-api.mjs - edit the C# doc comments instead.
---

# LanguageSpriteInstance

**Class** · namespace `VRFramework.Localization.Runtime` · assembly `VRFramework.Localization.Runtime` · [view source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/interaction/physics-hold/Runtime/Scripts/Language/Classes/Data/Sprite/LanguageSpriteInstance.cs#L17)

An asset holding translated sprites: one entry per image, each with a slot per language, plus
the images that are the same everywhere. Referenced by the Localization module and picked from
by every [Language] sprite field.

```csharp
[CreateAssetMenu(fileName = "SpriteList", menuName = "VRFramework/Language/SpriteList", order = 2)]
[Serializable]
public class LanguageSpriteInstance : ScriptableObject
```

**Inheritance:** [`object`](https://learn.microsoft.com/dotnet/api/system.object) ← [`Object`](https://docs.unity3d.com/ScriptReference/Object.html) ← [`ScriptableObject`](https://docs.unity3d.com/ScriptReference/ScriptableObject.html) ← `LanguageSpriteInstance`

## Fields

### languageEntries {#languageentries}

The translated sprites, one entry per image.

```csharp
[SerializeField]
public List<LanguageSpriteList> languageEntries
```

**Returns** `List<LanguageSpriteList>`

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/interaction/physics-hold/Runtime/Scripts/Language/Classes/Data/Sprite/LanguageSpriteInstance.cs#L22)

### untranslatedEntries {#untranslatedentries}

The sprites that are the same in every language.

```csharp
[SerializeField]
public List<UntranslatedSpriteElement> untranslatedEntries
```

**Returns** `List<UntranslatedSpriteElement>`

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/interaction/physics-hold/Runtime/Scripts/Language/Classes/Data/Sprite/LanguageSpriteInstance.cs#L24)

## Methods

### CheckForLanguageChanges() {#checkforlanguagechanges}

Rebuilds every entry's language slots against the project's current language list, keeping the
sprites already assigned. Run after a language is added, removed or reordered.

```csharp
public void CheckForLanguageChanges()
```

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/interaction/physics-hold/Runtime/Scripts/Language/Classes/Data/Sprite/LanguageSpriteInstance.cs#L76)

