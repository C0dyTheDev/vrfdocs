---
id: VRFBuildPreprocess
title: 'VRFBuildPreprocess class'
sidebar_label: 'VRFBuildPreprocess'
description: 'Prepares a build from the settings on the active build profile: the player settings it ships under, and the files in StreamingAssets the runtime reads back.'
# Generated by scripts/gen-api.mjs - edit the C# doc comments instead.
---

# VRFBuildPreprocess

**Class** · namespace `VRFramework.Core.Editors.Build` · assembly `VRFramework.Core.Editors` · [view source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Editor/Build/VRFBuildPreprocess.cs#L16)

Prepares a build from the settings on the active build profile: the player settings it ships
under, and the files in StreamingAssets the runtime reads back.

This used to open a modal window and make the person building pick a configuration, then
ask them whether to bump the version. Both answers are on the profile now, so a build
needs no one at the keyboard and a build server can run one.

```csharp
public class VRFBuildPreprocess : IPreprocessBuildWithReport, IOrderedCallback
```

**Inheritance:** [`object`](https://learn.microsoft.com/dotnet/api/system.object) ← `VRFBuildPreprocess`

**Implements:** [`IPreprocessBuildWithReport`](https://docs.unity3d.com/ScriptReference/Build.IPreprocessBuildWithReport.html), [`IOrderedCallback`](https://docs.unity3d.com/ScriptReference/Build.IOrderedCallback.html)

## Properties

### callbackOrder {#callbackorder}

Where this hook runs among Unity's build callbacks.

```csharp
public int callbackOrder { get; }
```

**Returns** [`int`](https://learn.microsoft.com/dotnet/api/system.int32)

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Editor/Build/VRFBuildPreprocess.cs#L19)

## Methods

### OnPreprocessBuild(BuildReport) {#onpreprocessbuild-buildreport}

Validates and applies the active profile's framework settings as a build starts. Errors stop
the build before anything is written.

```csharp
public void OnPreprocessBuild(BuildReport report)
```

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `report` | [`BuildReport`](https://docs.unity3d.com/ScriptReference/Build.Reporting.BuildReport.html) | Unity's report for the build being started. |

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Editor/Build/VRFBuildPreprocess.cs#L26)

