---
id: ProjectTags
title: 'ProjectTags class'
sidebar_label: 'ProjectTags'
description: 'Makes sure the tags and layers a setup depends on exist before the components that read them are added.'
# Generated by scripts/gen-api.mjs - edit the C# doc comments instead.
---

# ProjectTags

**Class** · namespace `VRFramework.Core.Editors.SceneBuilding` · assembly `VRFramework.Core.Editors` · [view source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Editor/SceneBuilder/Scene/ProjectTags.cs#L14)

Makes sure the tags and layers a setup depends on exist before the components that read them
are added. GrabbableObject.Reset assigns the "Grabbable" tag and layer, and Unity logs an
error for each one that is missing, so a project that has not run the package setup would
get two errors every time someone made an object grabbable.
The project setup writes the framework's tags and layers through here as well, so both the
one-off and the whole-project case go down the same path.

```csharp
public static class ProjectTags
```

**Inheritance:** [`object`](https://learn.microsoft.com/dotnet/api/system.object) ← `ProjectTags`

## Methods

### EnsureLayer(string) {#ensurelayer-string}

```csharp
public static void EnsureLayer(string layer)
```

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `layer` | [`string`](https://learn.microsoft.com/dotnet/api/system.string) |  |

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Editor/SceneBuilder/Scene/ProjectTags.cs#L42)

### EnsureTag(string) {#ensuretag-string}

```csharp
public static void EnsureTag(string tag)
```

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `tag` | [`string`](https://learn.microsoft.com/dotnet/api/system.string) |  |

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Editor/SceneBuilder/Scene/ProjectTags.cs#L21)

