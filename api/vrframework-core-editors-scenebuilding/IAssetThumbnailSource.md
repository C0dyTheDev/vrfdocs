---
id: IAssetThumbnailSource
title: 'IAssetThumbnailSource interface'
sidebar_label: 'IAssetThumbnailSource'
description: 'A source that carries its own card thumbnails, for entries that are not in the project yet and so have no Unity preview.'
# Generated by scripts/gen-api.mjs - edit the C# doc comments instead.
---

# IAssetThumbnailSource

**Interface** · namespace `VRFramework.Core.Editors.SceneBuilding` · assembly `VRFramework.Core.Editors` · [view source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/interaction/physics-hold/Editor/SceneBuilder/AssetSources/IAssetSource.cs#L132)

A source that carries its own card thumbnails, for entries that are not in the project yet
and so have no Unity preview. Implement alongside [`IAssetSource`](/api/vrframework-core-editors-scenebuilding/IAssetSource); the browser
polls [`IsThumbnailPending(AssetEntry)`](/api/vrframework-core-editors-scenebuilding/IAssetThumbnailSource#isthumbnailpending-assetentry) so a card fills itself in when the image lands.

```csharp
public interface IAssetThumbnailSource
```

**Derived:** [`AssetManagerSource`](/api/vrframework-core-editors-scenebuilding/AssetManagerSource)

## Methods

### GetThumbnail(AssetEntry) {#getthumbnail-assetentry}

Thumbnail for an entry, or null while it is still on its way.

```csharp
Texture2D GetThumbnail(AssetEntry entry)
```

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `entry` | [`AssetEntry`](/api/vrframework-core-editors-scenebuilding/AssetEntry) | Entry to picture. |

**Returns** [`Texture2D`](https://docs.unity3d.com/ScriptReference/Texture2D.html) - The thumbnail, or null.

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/interaction/physics-hold/Editor/SceneBuilder/AssetSources/IAssetSource.cs#L137)

### IsThumbnailPending(AssetEntry) {#isthumbnailpending-assetentry}

Whether the entry's thumbnail is still being fetched, so the card should keep polling.

```csharp
bool IsThumbnailPending(AssetEntry entry)
```

**Parameters**

| Name | Type | Description |
| --- | --- | --- |
| `entry` | [`AssetEntry`](/api/vrframework-core-editors-scenebuilding/AssetEntry) | Entry to check. |

**Returns** [`bool`](https://learn.microsoft.com/dotnet/api/system.boolean) - True while the thumbnail is on its way.

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/interaction/physics-hold/Editor/SceneBuilder/AssetSources/IAssetSource.cs#L142)

