---
id: Minigame
title: 'Minigame class'
sidebar_label: 'Minigame'
description: 'One part of a minigame session: its scenes, its clock, and where failing it leads.'
# Generated by scripts/gen-api.mjs - edit the C# doc comments instead.
---

# Minigame

**Class** · namespace `VRFramework.Minigames.Runtime` · assembly `VRFramework.Minigames.Runtime` · [view source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Minigames/MinigameSet.cs#L24)

One part of a minigame session: its scenes, its clock, and where failing it leads.

```csharp
[Serializable]
public class Minigame
```

**Inheritance:** [`object`](https://learn.microsoft.com/dotnet/api/system.object) ← `Minigame`

## Fields

### onFailedReturnToLevel {#onfailedreturntolevel}

Position of the part the trainee is sent back to when this one is failed.

```csharp
[Tooltip("V případě neúspěchu - do jaké úrovně hráče hra vrátí.")]
public int onFailedReturnToLevel
```

**Returns** [`int`](https://learn.microsoft.com/dotnet/api/system.int32)

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Minigames/MinigameSet.cs#L41)

### realTime {#realtime}

How long the part actually lasts, in seconds.

```csharp
public float realTime
```

**Returns** [`float`](https://learn.microsoft.com/dotnet/api/system.single)

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Minigames/MinigameSet.cs#L28)

### scenesToLoadNames {#scenestoloadnames}

Scenes this part may run in; one is picked at random. All of them need to be in the build.

```csharp
[Tooltip("Je nutno přidat scény do Build Indexu!")]
public string[] scenesToLoadNames
```

**Returns** [`string[]`](https://learn.microsoft.com/dotnet/api/system.string[])

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Minigames/MinigameSet.cs#L37)

### shownTime {#showntime}

What the countdown shows for that same duration, in seconds. Set differently from
[`realTime`](/api/vrframework-minigames-runtime/Minigame#realtime) to make the clock appear to run faster or slower than it does.

```csharp
public float shownTime
```

**Returns** [`float`](https://learn.microsoft.com/dotnet/api/system.single)

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Minigames/MinigameSet.cs#L33)

## Methods

### GetRandomSceneName() {#getrandomscenename}

Picks one of this part's scenes at random.

```csharp
public string GetRandomSceneName()
```

**Returns** [`string`](https://learn.microsoft.com/dotnet/api/system.string) - Name of the scene to load.

[View source](https://git.cie-group.cz/vr-framework/vrf4/core/-/blob/main/Runtime/Scripts/Minigames/MinigameSet.cs#L45)

